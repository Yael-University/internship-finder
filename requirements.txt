# Core scraping
selenium
webdriver-manager
undetected-chromedriver

# LLM fit scoring (Groq free tier)
langchain-core
langchain-groq

# ATS scoring — ML models (download once, run locally)
sentence-transformers>=3.0.0
transformers>=4.40.0

# Config / CLI
PyYAML~=6.0.2
inquirer
loguru>=0.7.2
pydantic

# API server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# Vector store
chromadb>=0.5.0

# Tests
pytest>=8.3.3
pytest-cov
